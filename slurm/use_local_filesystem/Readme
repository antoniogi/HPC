This batch script file and the associate bash script allow users of Stampede to use the local disk of each node (~70GB) instead of the parallel filesystem. This reduces the IO load on the parallel filesystem. The data transfer rate from Lustre to local disk is approx. 1GB per minute.
